name: Test Secret
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - name: Check if secret exists
        run: |
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "❌ OPENAI_API_KEY is NOT set"
            exit 1
          else
            echo "✅ OPENAI_API_KEY is set"
            echo "First 7 characters: ${OPENAI_API_KEY:0:7}"
          fi
```

実行すると、APIキーの最初の7文字 (例: `sk-proj`) だけが表示されます。

---

## 🚨 Secret が正しく動作しない場合のチェックリスト

### 1. **名前が完全一致しているか**
- ❌ `openai_api_key` (小文字)
- ❌ `OPENAI_API_KEY ` (末尾にスペース)
- ✅ `OPENAI_API_KEY` (完全一致)

### 2. **値にスペースや改行が混入していないか**
コピペ時に空白が入る場合があります:
```
# 悪い例
 sk-proj-abc123  ← 前後にスペース

# 良い例
sk-proj-abc123
